VITE_USE_API=false
VITE_API_BASE_URL=http://localhost:8080
# Only required if VITE_API_BASE_URL points at the deployed Cloud Run API
# (which enforces this) rather than a local `npm run dev` server (which
# doesn't, unless you set API_KEY there yourself).
VITE_API_KEY=
